<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.aireview.mapper.ResumeFileMapper">

    <insert id="insert" parameterType="com.example.aireview.domain.ResumeFile" useGeneratedKeys="true" keyProperty="fileId">
        insert into resume_file (original_filename, stored_filename, file_path, file_type, file_size)
        values (#{originalFilename}, #{storedFilename}, #{filePath}, #{fileType}, #{fileSize})
    </insert>

    <select id="findById" resultType="com.example.aireview.domain.ResumeFile">
        select file_id, original_filename, stored_filename, file_path, file_type, file_size, uploaded_at
        from resume_file
        where file_id = #{fileId}
    </select>

</mapper>
